% !TEX TS-program = lualatex
% !TEX encoding = UTF-8

% This is a simple template for a LuaLaTeX document using gregorio scores.

\documentclass[10pt]{article} % use larger type; default would be 10pt

% usual packages loading:
\usepackage{fontspec}
\usepackage{graphicx} % support the \includegraphics command and options
%\usepackage{geometry} % See geometry.pdf to learn the layout options. There are lots.
%\geometry{a4paper} % or letterpaper (US) or a5paper or....
%\usepackage[pass,paperwidth=4.6in,paperheight=15in]{geometry}
\usepackage{gregoriotex} % for gregorio score inclusion
\usepackage{fullpage} % to reduce the margins



\usepackage[papersize={\largeurinitiale,3cm},margin=0.1cm]{geometry}

%\textwidth 4.6in

\defaultfontfeatures{Scale=MatchLowercase}
\setmainfont{EB Garamond}
%\setromanfont{Garamond Premier Pro}
%\setmonofont[Scale=0.90,Ligatures=NoCommon]{Courier}

% to change the font to something better, you can install the kpfonts package (if not already installed). To do so
% go open the "TeX Live Manager" in the Menu Start->All Programs->TeX Live 2010

% here we begin the document
\begin{document}
	
\thispagestyle{empty}

% The title:
%\begin{center}\begin{huge}\textsc{Populus Sion}\end{huge}\end{center}

% Here we set the space around the initial.
% Please report to http://home.gna.org/gregorio/gregoriotex/details for more details and options
\setspaceafterinitial{2.2mm plus 0em minus 0em}
\setspacebeforeinitial{2.2mm plus 0em minus 0em}

% Here we set the initial font. Change 43 if you want a bigger initial.

\newfontface\vectis{Diploma}
\def\greinitialformat#1{%
{\color{red}\fontsize{55}{55}\vectis #1}}
%\def\greinitialformat#1{%
%{\fontsize{43}{43}\selectfont #1}%
%}

%\renewcommand{\LettrineFontHook}{\vectis}

%\def\gretextformat#1{%
%{\fontsize{14}{14}\selectfont #1}%
%}

\def\gretranslationformat#1{%
{\fontsize{10.5}{10.5}\selectfont \textit{#1}}%
\relax %
}

% We set red lines here, comment it if you want black ones.
\redlines

% We set VII above the initial.
\gresetfirstlineaboveinitial{\small \textsc{\textbf{\aboveinitial}}}{\small \textsc{\textbf{\aboveinitial}}}

% We type a text in the top right corner of the score:
%\commentary{{\small \emph{Cf. Is. 30, 19 . 30 ; Ps. 79}}}

%\grespaceabovelines pour l'espace au dessus des lignes ;
%\grespacelinestext pour l'espace séparant la ligne la plus basse et le bas du texte ;
%\grespacebeneathtext pour l'espace en dessous du texte ;
%\gretranslationheight pour l'espace dédié à la traduction le cas échéant.

% and finally we include the score. The file must be in the same directory as this one.
\begin{large}
\includescore{source.tex}
\end{large}


\end{document}


% pdfjam test.pdf --papersize '{14cm,2.6cm}'
% pdftk test-pdfjam.pdf burst output test-%d.pdf